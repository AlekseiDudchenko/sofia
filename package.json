{
  "name": "sofia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Тренажёр таблицы умножения",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/stamp-version.mjs",
    "watch": "tsc -p tsconfig.json --watch",
    "start": "node scripts/dev-server.mjs",
    "test": "node --test test/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "typescript": "^5.9.3"
  }
}
